import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    value: 8
}

export const homeValue2Slice = createSlice({
    name: "homeValue2",
    initialState,
    reducers: {

    }
})

export default homeValue2Slice.reducer;